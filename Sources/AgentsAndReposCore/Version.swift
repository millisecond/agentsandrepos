public enum Version {
    public static let current = "0.2.0"
}

public enum AppInfo {
    /// User-visible product name. The binary/formula stays `agentsandrepos`.
    public static let displayName = "Agents & Repos"
}
